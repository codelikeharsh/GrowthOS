{
  "name": "zero2one-growth-os",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "preinstall": "node scripts/check-runtime.cjs",
    "dev": "node --env-file=.env ./node_modules/turbo/bin/turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write . && turbo run format",
    "format:check": "prettier --check . && turbo run format:check",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:integration": "turbo run test:integration",
    "test:e2e": "turbo run test:e2e",
    "db:generate": "pnpm --filter @growthos/db generate",
    "db:validate": "pnpm --filter @growthos/db validate",
    "db:migrate": "pnpm --filter @growthos/db migrate",
    "db:migrate:deploy": "pnpm --filter @growthos/db migrate:deploy",
    "db:studio": "pnpm --filter @growthos/db studio",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "clean": "turbo run clean && rimraf .turbo coverage",
    "prepare": "node scripts/check-runtime.cjs"
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "rimraf": "6.0.1",
    "turbo": "2.5.8",
    "typescript": "5.9.3"
  }
}
