# Terraform

Production infrastructure is deliberately deferred until Phase 10. This directory records the
accepted AWS/Terraform boundary without introducing empty modules or deployable placeholders.
